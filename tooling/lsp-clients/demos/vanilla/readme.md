# Vanilla TS Demo

Vanilla TS integration: `import { createKsonEditor } from '@kson/monaco-editor'`.
See [`../readme.md`](../readme.md) for the full set of integration examples.

```bash
./gradlew tooling:lsp-clients:npm_run_demoVanilla
```

Serves on [http://localhost:5174](http://localhost:5174).
