Important: this subproject only exists because the
`id("org.graalvm.buildtools.native")` Gradle plugin is incompatible with
`kotlin("multiplatform")`. If the situation changes in the future, we could
move the build logic to the top-level `build.gradle.kts`.
